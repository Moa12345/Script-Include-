Feature	Purpose	Client/Server	Example
UI Action	Performs an action when a user clicks a button, link, or context menu item	Client-side, Server-side, or Both	Create a Change Request from an Incident, Close Incident button
UI Policy	Dynamically controls form behavior (mandatory, visible, read-only)	Client-side	Make "Close Notes" mandatory when State = Resolved
Data Policy	Enforces data rules regardless of how data is entered	Server-side	Ensure "Assignment Group" is mandatory whether records are created from UI, Import Set, REST API, etc.
